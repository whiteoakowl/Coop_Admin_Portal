<!doctype html>
<html lang="en">
<head>
  <%- include('partials/head', { title }) %>
</head>
<body>
  <%- include('partials/portal-nav', { portalTitle: 'Student Portal' }) %>

  <main id="main-content" tabindex="-1" class="admin-main">
    <h1>Name Tag</h1>
    <p class="hint">
      Print your own name tag, using the design Main Admin/Co-op Admin has set up. You can't change the design here.
    </p>

    <% if (error) { %><div class="alert alert-error"><%= error %></div><% } %>

    <% if (!member) { %>
      <p class="roster-empty">No student profile found for your account.</p>
    <% } else { %>
      <form method="POST" action="/student/name-tag/print" target="_blank">
        <p><strong><%= member.name %></strong></p>
        <button type="submit" class="primary-btn">Print My Name Tag</button>
      </form>
    <% } %>
  </main>
</body>
</html>
