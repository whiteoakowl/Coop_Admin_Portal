<!doctype html>
<html lang="en">
<head>
  <%- include('partials/head', { title }) %>
</head>
<body>
  <%- include('partials/portal-nav', {
    portalTitle: 'Main Admin',
  }) %>

  <main id="main-content" tabindex="-1" class="admin-main">
    <h1>Main Admin</h1>
    <p class="hint">The control center for the whole platform - users, roles/permissions, and public website content.</p>

    <%# A real request: "should show a counter of how many families,
        how many parents and how many students." %>
    <div class="totals-card">
      <div class="totals-item">
        <span class="stat-value"><%= familyCount %></span>
        <span class="stat-label">Families</span>
      </div>
      <div class="totals-item">
        <span class="stat-value"><%= parentCount %></span>
        <span class="stat-label">Parents</span>
      </div>
      <div class="totals-item">
        <span class="stat-value"><%= studentCount %></span>
        <span class="stat-label">Students</span>
      </div>
    </div>

    <div class="totals-card">
      <div class="totals-item">
        <span class="stat-value"><%= pendingCount %></span>
        <span class="stat-label">Pending Accounts</span>
      </div>
      <div class="totals-item">
        <span class="stat-value"><%= activeCount %></span>
        <span class="stat-label">Active Accounts</span>
      </div>
    </div>

    <% if (pendingCount > 0) { %>
      <div class="alert alert-info">
        <%= pendingCount %> account<%= pendingCount === 1 ? '' : 's' %> awaiting approval.
        <a href="/main-admin/users">Review now &rarr;</a>
      </div>
    <% } %>

    <section class="portal-dashboard-section">
      <h2>Accounts by Role</h2>
      <% if (roleCounts.length === 0) { %>
        <p class="roster-empty">No roles have been granted yet.</p>
      <% } else { %>
        <div class="totals-card">
          <% roleCounts.forEach(function(r) { %>
            <div class="totals-item">
              <span class="stat-value"><%= r.c %></span>
              <span class="stat-label"><%= r.label %></span>
            </div>
          <% }); %>
        </div>
      <% } %>
    </section>

    <%# A real request: "roles and permissions, website settings, and
        co-op admin portal links should all be under the main admin
        settings gear icon" - moved to the gear icon's own dropdown
        (views/partials/portal-nav.ejs), alongside Users which already
        lived there. Users also keeps this one homepage card since
        approving pending registrations is common enough day-to-day work
        to still surface here directly. %>
    <div class="portal-dashboard-grid">
      <section class="portal-dashboard-card">
        <h2>Users</h2>
        <p class="hint">Approve pending registrations, create accounts directly, and assign portal roles.</p>
        <a class="roster-action-btn" href="/main-admin/users">Manage Users</a>
      </section>
    </div>
  </main>
</body>
</html>
