<!doctype html>
<html lang="en">
<head>
  <%- include('partials/head', { title }) %>
</head>
<body>
  <%- include('partials/portal-nav', {
    portalTitle: 'Store',
    navLinks: [
      { href: '/portal', icon: 'home', label: 'My Portals' },
      { href: '/store', icon: 'tag', label: 'Store' },
      { href: '/store/orders', icon: 'file-text', label: 'My Orders' },
    ],
  }) %>

  <main id="main-content" tabindex="-1" class="admin-main">
    <h1>Store</h1>

    <% if (products.length === 0) { %>
      <p class="roster-empty">Nothing for sale online right now.</p>
    <% } else { %>
      <%# A real request: "change shop product card to look similar [to a
          reference design]" - photo on the left, an In Stock/Out of
          Stock badge, price and stock count on one row, and a prominent
          order button, replacing the old top-image/plain-text
          .event-card layout this page borrowed before. %>
      <div class="store-product-grid">
        <% products.forEach(function(p) { %>
          <a class="store-product-card" href="/store/<%= p.id %>">
            <% if (p.imageUrl) { %>
              <img class="store-product-card-image" src="<%= p.imageUrl %>" alt="" />
            <% } %>
            <div class="store-product-card-body">
              <div class="store-product-card-header">
                <h3><%= p.name %></h3>
                <span class="badge-pill <%= p.inStock ? 'badge-pill-green' : 'badge-pill-gray' %>"><%= p.inStock ? 'In Stock' : 'Out of Stock' %></span>
              </div>
              <div class="store-product-card-price-row">
                <span class="store-product-card-price">$<%= (p.price_cents / 100).toFixed(2) %></span>
                <% if (p.stockCount != null) { %>
                  <span class="store-product-card-divider"></span>
                  <span class="store-product-card-stock"><%= p.stockCount %> in stock</span>
                <% } %>
              </div>
              <span class="store-product-card-cta">View &amp; Order <svg class="icon"><use href="#icon-arrow-right"/></svg></span>
            </div>
          </a>
        <% }); %>
      </div>
    <% } %>
  </main>
</body>
</html>
