<!doctype html>
<html lang="en">
<head>
  <%- include('partials/head', { title }) %>
</head>
<body>
  <%- include('partials/portal-nav', {
    portalTitle: 'Main Admin',
  }) %>

  <main id="main-content" tabindex="-1" class="admin-main">
    <h1><%= submission.memberName %></h1>
    <p class="hint">Submitted <%= submission.submitted_at %></p>
    <p><a href="/main-admin/forms/<%= form.id %>/submissions">&larr; All Submissions</a></p>

    <% answers.forEach(function(a) { %>
      <div class="manage-section">
        <div class="grid-box-header"><h2><%= a.fieldLabel %></h2></div>
        <% if (a.fieldType === 'multiple_choice') { %>
          <p><%= a.choiceLabels.length ? a.choiceLabels.join(', ') : '(no selection)' %></p>
        <% } else if (a.fieldType === 'checkbox') { %>
          <p><%= a.value_text === '1' ? 'Yes' : 'No' %></p>
        <% } else if (a.fieldType === 'file') { %>
          <% if (a.value_text) { %><p><a href="/forms/files/<%= a.id %>">Download File</a></p><% } else { %><p class="hint">No file uploaded.</p><% } %>
        <% } else { %>
          <p><%= a.value_text || '(blank)' %></p>
        <% } %>
      </div>
    <% }); %>
  </main>
</body>
</html>
