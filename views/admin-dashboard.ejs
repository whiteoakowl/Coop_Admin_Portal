<!doctype html>
<html lang="en">
<head>
  <%- include('partials/head', { title }) %>
</head>
<body class="admin-page">
  <%- include('partials/admin-nav') %>
  <main id="main-content" tabindex="-1" class="admin-main">
    <h1>Dashboard</h1>

    <div class="dashboard-stat-panel">
      <div class="dashboard-stat-grid">
        <div class="dashboard-stat-card dashboard-stat-orange">
          <div class="dashboard-stat-icon"><svg class="icon"><use href="#icon-users"/></svg></div>
          <span class="dashboard-stat-label">Total Monday Parents</span>
          <span class="dashboard-stat-value"><%= mondayParentCount %></span>
        </div>
        <div class="dashboard-stat-card dashboard-stat-blue">
          <div class="dashboard-stat-icon"><svg class="icon"><use href="#icon-graduation-cap"/></svg></div>
          <span class="dashboard-stat-label">Monday Students</span>
          <span class="dashboard-stat-value"><%= mondayStudentCount %></span>
        </div>
        <div class="dashboard-stat-card dashboard-stat-purple">
          <div class="dashboard-stat-icon"><svg class="icon"><use href="#icon-users"/></svg></div>
          <span class="dashboard-stat-label">Wednesday Parents</span>
          <span class="dashboard-stat-value"><%= wednesdayParentCount %></span>
        </div>
        <div class="dashboard-stat-card dashboard-stat-blue">
          <div class="dashboard-stat-icon"><svg class="icon"><use href="#icon-graduation-cap"/></svg></div>
          <span class="dashboard-stat-label">Wednesday Students</span>
          <span class="dashboard-stat-value"><%= wednesdayStudentCount %></span>
        </div>
        <div class="dashboard-stat-card dashboard-stat-green">
          <div class="dashboard-stat-icon"><svg class="icon"><use href="#icon-home-heart"/></svg></div>
          <span class="dashboard-stat-label">Total Families</span>
          <span class="dashboard-stat-value"><%= familyCount %></span>
        </div>
        <div class="dashboard-stat-card dashboard-stat-blue">
          <div class="dashboard-stat-icon"><svg class="icon"><use href="#icon-graduation-cap"/></svg></div>
          <span class="dashboard-stat-label">Total Students</span>
          <span class="dashboard-stat-value"><%= studentCount %></span>
        </div>
        <div class="dashboard-stat-card dashboard-stat-orange">
          <div class="dashboard-stat-icon"><svg class="icon"><use href="#icon-users"/></svg></div>
          <span class="dashboard-stat-label">Total Parents</span>
          <span class="dashboard-stat-value"><%= parentCount %></span>
        </div>
      </div>
    </div>

    <h2 class="dashboard-section-title">Today's Attendance</h2>
    <div class="dashboard-analytics-grid">
      <%- include('partials/dashboard-type-stats', { label: 'Parents', stats: parentStats }) %>
      <%- include('partials/dashboard-type-stats', { label: 'Students', stats: studentStats }) %>
    </div>

    <h2 class="dashboard-section-title">Alert Log</h2>
    <div class="alert-log">
      <% if (alerts.length === 0) { %>
        <p class="roster-empty">No active alerts today.</p>
      <% } else { %>
        <ul class="alert-log-list">
          <% alerts.forEach(function(a) { %>
            <li class="alert-log-item alert-log-item-<%= a.severity %>">
              <svg class="icon alert-log-icon"><use href="#<%= a.severity === 'danger' ? 'icon-x-circle' : 'icon-info-circle' %>"/></svg>
              <div class="alert-log-body">
                <span class="alert-log-day"><%= a.dayLabel %></span>
                <span class="alert-log-message"><a href="<%= a.link %>"><%= a.message %></a></span>
              </div>
            </li>
          <% }); %>
        </ul>
      <% } %>
    </div>
  </main>
</body>
</html>
